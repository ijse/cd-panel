{
  "name": "repo",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir dist",
    "deploy": "echo $TARGET > dist/release"
  },
  "license": "MIT",
  "dependencies": {
    "left-padding": "^1.0.0"
  }
}
